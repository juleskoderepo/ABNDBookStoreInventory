<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.abndbookstoreinventory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_book_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_book_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InventoryActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".InventoryActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".InventoryActivity"/>
        </activity>

        <provider
            android:name=".data.ProductProvider"
            android:authorities="com.example.android.abndbookstoreinventory"
            android:exported="false" />

    </application>

</manifest>